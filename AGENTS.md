# Agent Workflow Notes

- Use git branches for task work.
- Do not create git worktrees unless the user explicitly asks for one.
- Do not run `npm run check:e2e` in this repo unless that script is added; it does not currently exist.
